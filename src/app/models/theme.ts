export class Theme {

    id: number;
    label: string;
    name: string;

    constructor(id: number, label: string, name: string) {
        this.id = id;
        this.label = label;
        this.name = name;
    }
}
