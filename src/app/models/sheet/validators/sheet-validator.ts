import {InputSheet} from '../input';
import {OutputSheet} from '../output';

export class SheetValidator {

  public isValidInputSheet(sheet: InputSheet): boolean {
    return true;
  }

  public isValidOutputSheet(sheet: OutputSheet): boolean {
    return true;
  }
}
