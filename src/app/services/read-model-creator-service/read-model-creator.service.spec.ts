/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ReadModelCreaterService } from './read-model-creator.service';

describe('ReadModelCreaterService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ReadModelCreaterService]
    });
  });

  // it('should ...', inject([ReadModelCreaterService], (service: ReadModelCreaterService) => {
  //   expect(service).toBeTruthy();
  // }));
});
